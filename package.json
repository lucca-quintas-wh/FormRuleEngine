{
  "name": "form-rule-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Motor declarativo de comportamento de formulário para projetos legados — regras como atributos HTML.",
  "scripts": {
    "test": "node tests/smoke.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
